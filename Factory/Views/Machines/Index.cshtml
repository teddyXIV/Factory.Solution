@using Factory.Models;

<h2>Machines</h2>

@if (@Model.Count == 0)
{
    <h4>No machines have been purchased.</h4>
}

@foreach (Machine m in Model)
{
    @Html.ActionLink($"{m.Name}", "Details", new { id = m.MachineId })
}

@Html.ActionLink("Add a new machine", "Create")